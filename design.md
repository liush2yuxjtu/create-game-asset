# 江湖 · 俯瞰特效试验场

状态：可实现合同；实际验收以 `docs/verification/2026-09-23-topdown-playground.md` 及运行脚本输出为准。创建日期：2026-09-23。入口：`/playground/`。作者：仓库内原创程序化实现。

## 1. 真实意图与范围

让使用者在同一片俯瞰地面、相同角色尺度、可改变的落点上，通览所有现有技能源码，选一招反复试放，再把自己的自包含 `.js` 插入同一个场景。主要任务是看技能动态与空间关系，不新增装备、数值养成或战斗系统。

主动作：**从图鉴选招 → 在场景点击落点 → 播放 / 慢放 / 倒退采样**。次动作：切换明暗地形、移动施法者、调旋转/缩放、读源码、导入本地技能。模拟角色和木人仅提供尺度与遮挡，没有伤害结算。

保持既有页面、青岚GLB和3.2秒V2合同、纸鹤4.8秒动画、36式渲染器及其控制器不变。新入口是在旧能力上增加场景宿主，不把历史资产改写成另一种运行时。

## 2. 名称与来源边界

本次《一念江湖》指 **天羽游戏 / wangguoqing / TapTap app 218252**。仓库既有 `design-systems/ynjh/` 明确是一念逍遥，继续保留，不能覆盖或混用。详见 [来源台账](public/playground/sources.json)。

|来源|证据级别|可使用的观察|不可推导的结论|
|---|---|---|---|
|[JH-01 TapTap官方商店](https://www.taptap.cn/app/218252)|已读开发者正文|单机RPG，预判Boss技能与走位|真实粒子、弹速、渲染技术|
|[JH-02 好游快爆](https://www.3839.com/a/134797.htm)|已看两张地图原图；已读日志|俯瞰地面、块状地形、小型直立像素角色、地面路线|精确3D倾角、完整技能时序|
|[JH-03 LoveKun原始攻略](https://www.taptap.cn/moment/158154480636921330)|已读玩法说明；已看火鸟图|冰鸟的前方落点、火鸟的脚下范围；静态明黄色放射状表现|原游戏完整动画、所有版本通用CD|
|[JH-04 原作者视频](https://www.taptap.cn/moment/160445921623016902)|元数据已读；未播放|真实视频候选的出处|动态parity或逐帧测量|

时间边界：JH-03是2021-06-27攻略，而JH-02的后续更新提到冰鸟CD变长。不能将历史“6秒/3秒”直接写为当前技能参数。二手页面2025/2026编辑日期也不等于新游戏版本。台账中转载项不重复计为独立证据。

本次不分发原游戏图像、视频、贴图或APK；不会把我们画的场景冒充游戏截图。所有新增尺寸、配色、时长、符号和形状均为 **proposed / 本项目原创选择**。动态对标 **NOT_RUN**；用户美术批准 **PENDING**。

## 3. 空间与可读性合同

- 宿主世界：960×600逻辑像素，原点左上，+X向右，+Y向下。固定2D俯瞰空间；不是3D透视镜头，也未测量原游戏倾角。
- 角色以脚底为位置锚点，图形从脚底向上延伸；地面、后排角色、调试圈、技能、前排角色、标签按顺序绘制。角色按脚底Y排序。固定mock障碍只用于视觉裁切/遮挡压力，不执行碰撞和寻路。
- 技能模块坐标：透明Canvas 480×320，中心(240,160)。宿主先把模块绘到离屏Canvas，再按落点、旋转和缩放合成。模块不得清除/涂改宿主地面。
- 调试范围圈与施法者到落点连线由宿主绘制，只显示空间参考，明确不是实际命中范围或原游戏预警。技能结束时清除圈和线；地面、角色和落点十字仍在。
- 既有36式内部的一些椭圆透视仍属于原画法，宿主不会通过拉伸伪装成重新制作的正交3D特效。中心插件为局部范围研究，不自动产生“施法者到目标”的真实弹道。
- 一次只播放当前招式；全局图鉴为固定49%采样，不同时驱动40个动画，不用发光强度代替运动设计。

### 五种fixture

|ID|场景|验证意图|
|---|---|---|
|courtyard|青石试剑庭|单目标、基准角色尺度、边缘清楚|
|crowd|八方木人阵|8目标、范围占据、前后遮挡|
|bamboo|竹径窄巷|狭窄可用空间、墙边和场景边缘裁切|
|snow|月白雪庭|浅色地面上的亮部、粒子轮廓|
|night|夜泊深潭|深色地面上的亮度与泛光|

`fixtures.js` 为确定性数据和原创Canvas绘制。噪声只取决于坐标，不按帧随机；同一状态、同一时间必须可重现。

## 4. 页面结构与视觉变量（均为项目提案）

信息顺序：品牌导航 → 当前任务与数量 → 大场景和紧邻的参数区 → 本地JS插槽 → 全局图鉴 → 历史运行时 → 全源码索引 → 证据台账。桌面场景占主体宽度，控制面板约280px。窄屏场景在上、参数在下；画布保持8:5，不让固定HUD盖住技能。

|变量|值|角色|
|---|---|---|
|paper|#f3f0e6|页面底色，不争夺画布焦点|
|ink|#203b33|标题、正文、主按钮|
|muted|#69766a|来源、说明和次级文字|
|rule|#cfcec0|面板与信息分区|
|sage|#577d66|当前选择、控制反馈|
|gold|#b69657|轻量重点与空间标记|
|stage|#142b27|画布容器与播放器|
|display type|Songti SC / STSong / serif|中文标题，系统回退，不分发字体|
|body type|system-ui / PingFang SC / Microsoft YaHei|控件与正文|
|code type|ui-monospace / SFMono-Regular / Consolas|SHA、时序、源码|

所有交互均有文字名、可见focus outline、标签；图鉴卡使用button而非只可点的div。主操作与文件输入至少44px；次级采样按钮允许较紧凑尺寸。初始暂停，减少运动偏好下取消平滑滚动。Canvas支持方向键移动施法者，手机可通过模式选择改变点击含义。桌面窄视口测试不等同屏幕阅读器或真机验收。

## 5. 插件与运行时合同

目前40个兼容入口 = 36个既有变体 + 独立初版雨锋 + 3个原创空间研究。36个变体各自有`.js`描述，但共用`variant-engine.js`，不是36个自包含引擎。

新样件：足下流火3.6秒、前路凝霜3.5秒、青玉守御4.0秒。仅参考“地面范围/前路阻断/护域”的意图，时长、几何和颜色不是原游戏采样。

```js
// public/playground/examples/my-effect.js
export default {
  id: 'my-effect', name: '我的技能', duration: 3,
  family: 'spatial', familyName: '落点研究',
  description: '聚势 → 展开 → 淡出', motion: '范围展开',
  draw(ctx, seconds, options = {}) {
    if (options.visible === false || seconds <= 0 || seconds >= 3) return;
    ctx.save();
    ctx.translate(240, 160);
    ctx.globalAlpha = Math.min(1, seconds * 3, (3 - seconds) * 3);
    ctx.strokeStyle = '#a7d9be';
    ctx.beginPath(); ctx.arc(0, 0, 70, 0, Math.PI * 2); ctx.stroke();
    ctx.restore();
  }
};
```

`draw`是时间采样，不管理RAF、DOM、存储、事件或真实战斗。ID小写英文字母起始，后接字母数字连字符，最长64；name非空最长80；duration为有限正数且≤30秒；每帧save/restore，首尾透明，不累积状态。

两种接入：

1. **长期入库**：新增模块，加入`registry.js`；构建自动发现public/src源码。支持本仓库相对依赖，保留来源和许可证。运行完整verify后提交。
2. **本地试放**：页面选择/拖放≤64KB、自包含、default导出的`.js`。只在本次页面内运行，不上传、不自动写Git、不加载其相对依赖。文件代码运行在无same-origin权限的sandbox iframe所创建的Worker；CSP禁止外部网络，不能访问宿主DOM、cookie和localStorage。加载2.5秒/单帧1.5秒超时会移除隔离环境。错误显示原因并恢复仓库技能。此隔离不是恶意代码安全认证；只运行可信代码。

独立纸鹤`drawCrane`、四项`drawStudy`自带完整场景，青岚使用WebGL，均在索引中保留实际运行入口，不擅自改造成透明插件。`source-index.json`构建时列举所有public和src的JS，标记技能、播放器和支撑模块；源码存在不代表符合插件合同。

## 6. 时钟、交互与分享

RAF按实际delta秒推进；单次delta上限0.1秒，避免隐藏恢复时跳跃，不用于性能或精确离线测时。暂停/精确采样不触发玩法副作用；进度点0/20/49/80/100%只是跨不同duration的归一化探针，不声称各招阶段边界相同。0.25/0.5/1/2×由宿主控制。隐藏标签页暂停；非循环在末帧停止；循环通过duration取余。

点击改变落点，Shift＋点击或点按模式改变施法者；方向键移动12px。缩放0.6–1.8，朝向0–360°。链接保存仓库技能、fixture、采样进度、落点、施法者、缩放、角度及显示开关；读取时按白名单和范围校验，不执行URL代码。本地插件不编码到分享链接，不保存到localStorage。

## 7. 验收与明确不验收项

机器：`npm ci && npm run verify`，新增合同、源码覆盖、边界采样和文件语法测试；保持已有资产哈希与确定性打包。

浏览器：`python3 scripts/verify-playground.py --url http://127.0.0.1:4196/playground/ --out verification/playground-local`。使用真实Chromium，40项逐项0/20/49/80/100%取样，检查非空、差异、末帧清空和倒退复现；5fixture；真实播放/暂停/循环；落点/角色/缩放/旋转/层开关；过滤空状态；本地有效模块、无效合同、超时恢复；390×844与桌面；应用console错误。保存截图、连续播放帧、JSON与源SHA。

发布：Pages工作流先verify，再部署。读线上`build-info.json`，核对成功部署SHA、dirty=false，并在公共`/playground/`重复浏览器验收。验证直接源码与原青岚ZIP可获取。只提交代码或HTTP200不能算交互PASS。

不验收：原游戏完整动态parity、所有互联网来源、用户美术批准、原游戏素材许可、真实伤害/碰撞/寻路、Godot/Unity/UE接入、真机性能、所有辅助技术、任意不可信JS安全性。上述项目均不能由本试验场的技术PASS替代。
