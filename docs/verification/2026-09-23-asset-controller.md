# 外部资产控制器验证

Runtime verdict: PASS — 范围为全局/单实例控制、实际图层变化、本机配置持久化与无效配置恢复，不包含引擎、美术或公网页面验收。

Candidate: 748bab5；生产构建；本机 `http://127.0.0.1:4193/asset-lab/`，Codex in-app browser。同一轮开发中先对实现工作树采样七层，随后该提交构建重载复核覆盖、继承、数值、窄屏。

- 全局visible关闭：两个画布仅剩背景；A单项visible=true：只恢复A。刷新后维持A显示/B隐藏。最终版本再清除A覆盖：两项均继承全局隐藏。
- 1.80s七个绘制层分别关闭/恢复：主体、地面纹、尾迹、粒子、柔光、脉冲、墨笔。截图画布区域分别产生非空像素差；不是仅检查checkbox。
- B朱砂+0.7范围覆盖：A仍保持月白/原范围，截图可见不同颜色与尺寸；有效JSON应用后刷新保留。最终交付示例A恢复青玉，B朱砂/0.7范围/0.5倍速。
- B motion关闭：A从2.05推进到2.53，B保持1.80。独立速度示例：同次重播A显示0.22，B显示0.11；仅证明不同推进速度，非墙钟精确倍率认证。
- 相邻探针：JSON先包含visible=false再包含非法speed=99，页面拒绝并保留两项显示，证实无部分应用。单测覆盖false/0优先级、恢复继承、未知能力/ID、版本/范围错误及原子恢复。
- 非循环从头播放最终两项停在4.20且按钮恢复“播放”；重新定位1.80恢复效果。循环交付启用，未宣称完整参考技能逐帧对标。
- 390×844：文档宽度375，不超视口390；实际截图画布与控件可见。检查后重置视口。
- console warn/error捕获为空。
- 8项机器测试全部通过，原青岚资产与ZIP完整性、设计技能哈希、生产构建通过；仍有既有>500KB构建警告。

证据：本次工作区 outputs/asset-lab/ 下 all-on.png、七个图层-off.png、palette-override.png、final-head-override.png、mobile.png、zero-intensity.png。原始截图保留在任务交付目录，未把个人路径或会话资料写入仓库。

配置文本导入及刷新已验证；系统文件选择器上传和下载对话框未单独验收。参考parity、用户美术批准、Godot、真机性能：NOT_RUN。现有crane.js未修改。预览标签与本机4193服务保留供用户使用；用户自有页面未关闭。
